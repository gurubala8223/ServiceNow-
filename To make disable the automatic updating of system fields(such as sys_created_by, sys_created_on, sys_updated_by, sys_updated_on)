var gr = new GlideRecord('incident');
gr.initialize();
gr.autoSysFields(false); // Prevent system fields from being updated
gr.short_description = "Test incident without updating system fields";
gr.insert();
